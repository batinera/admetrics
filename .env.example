# Modo demonstração sem API (mock). Quando false, precisas da API (repo admetrics-back) em http://localhost:3001 e Postgres.
VITE_USE_MOCK=false

# Opcional: API noutro domínio (produção). Em dev deixe vazio para usar o proxy do Vite.
# Ex. Render (serviço admetrics-api): https://admetrics-api.onrender.com
# VITE_API_BASE_URL=https://admetrics-api.onrender.com
